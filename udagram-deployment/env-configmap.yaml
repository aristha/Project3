apiVersion: v1
kind: ConfigMap
metadata:
  name: env-configmap
  labels:
    app: my-app
data:
  POSTGRES_HOST: localhost
  POSTGRES_DB: postgres
  AWS_BUCKET: arn:aws:s3:::my-825566562884-bucket
  AWS_REGION: us-east-1
  AWS_PROFILE: test
  JWT_SECRET: testing
  URL: http://10.106.86.252:8100
  config: |
    address-pools:
      - name: default
        protocol: layer2
        addresses:
        - 192.168.1.240-192.168.1.250

